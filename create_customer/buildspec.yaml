version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - zip -r create_customer.zip create_customer/*
  #post_build:
    #commands:
      # - command
      # - command
#artifacts:
  #files:
  #  - 'create_customer/*'
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: create_customer
#cache:
  #paths:
    # - paths